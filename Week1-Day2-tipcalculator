#If the bill was $150.00, split between 5 people, with 12% tip. 

#Each person should pay (150.00 / 5) * 1.12 = 33.6
#Format the result to 2 decimal places = 33.60

#Tip: There are 2 ways to round a number. You might have to do some Googling to solve this.💪

#Write your code below this line 👇

print("Welcome to the tip calculator.")
bill = float(input("What was the total bill? $"))
int_bill = float(bill)
tip = input("what percentage tip would you like to give? 10, 12, 15 or 20? ")
int_tip = int(tip)
amount_at_table = input("How many people to split the bill? ")
int_amount = int(amount_at_table)
pay = (int_bill * (1 + (int_tip / 100)) / int_amount)
roundpay = "{:.2f}".format(pay)
print(f"Each person should pay: ${roundpay}")
